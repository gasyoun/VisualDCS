# M1 parser output — regenerable staging JSONL (can be large). Not committed.
staging/
# M2/M6 SQLite masters (pilot dcs.sqlite + full dcs_full.sqlite) — regenerable; not committed.
*.sqlite
*.sqlite.gz
# M4 exports — regenerable CSVs from the master (the reports/ findings ARE committed).
exports/
# M7 widgets — regenerable dashboard JSON from the master (the reports/ findings ARE committed).
widgets/
# Python bytecode cache
__pycache__/
