# M5 validation — DCS 2026 pilot pipeline

_Generated by `validate.py` over `dcs.sqlite` (pilot)._

**Result: ✅ ALL PASS**

| check | result | detail |
|---|:--:|---|
| Cross-walk at scale | ✅ | 14268 sentences, 0 mismatches |
| Referential integrity | ✅ | orphan_tokens=0, orphan_sentences=0, orphan_chapters=0, null_occ_id=0 |
| Idempotency (full re-run) | ✅ | identical data hash |
| Spot checks (full token) | ✅ | 50 verses, 0 mismatches |

Idempotency data hash: `5bf5237951491dfe…` (both runs).

## Notes

- **Cross-walk** confirms the master preserves the exact CoNLL-U `LemmaId` order per sentence.
- **Idempotency** rebuilds the pilot DB twice and hashes every data table (excluding the timestamped `provenance`); identical → deterministic.
- **Spot checks** compare every token field (form, lemma_id, upos, head, deprel, all FEATS) on 50 random verses.
- Scope is the pilot (13 texts); M6 runs the same suite over all 270.

