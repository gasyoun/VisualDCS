_Created: 06-06-2026 · Last updated: 05-09-2026_

# M5 validation — DCS 2026 pilot pipeline (13 texts)

_Generated by `validate.py` over `dcs.sqlite`._

**Result: ✅ ALL PASS**

| check | result | detail |
|---|:--:|---|
| Cross-walk at scale | ✅ | 14,268 sentences, 0 mismatches |
| Referential integrity | ✅ | orphan_tokens_lemma=0, orphan_tokens_sent=0, orphan_sentences=0, orphan_chapters=0 |
| Idempotency (full re-run) | ✅ | identical data hash |
| Spot checks (full token) | ✅ | 50 verses, 0 mismatches |

Idempotency data hash: `60a4d07156ad9d30…` (both runs).

## Notes

- **Cross-walk** is **position-based** (i-th sentence of each text) — the corpus reuses `sent_id` even within a chapter, so it is not a key; the master keys sentences by a synthetic id.
- **Spot checks** compare every token field (form, lemma_id, upos, head, deprel, all FEATS).
- Scope: pilot pipeline (13 texts).

_Dr. Mārcis Gasūns_
