# Project Objective: Import current DCS CoNLL-U updates into the relational export (pilot-first)

<!-- Replace the bracket above with the current high-level goal for this session. -->

## ➡️ Next Steps (Queue)

- [ ] **M0 — finish standing up the 2026 CoNLL-U.** The data repo `gasyoun/dcs-conllu` (pinned
      `04e0778`, 2026-03-05) is being pushed from `C:\Users\user\Documents\GitHub\dcs-conllu`. Once it's
      on GitHub, mount it: `git submodule add <url> src/DCS-data-2026/conllu`, then commit the VisualDCS
      reorg.
- [ ] **M1–M5 pilot** per `src/DCS-data-2026/DCS_CONLLU_IMPORT_PLAN.md`: full CoNLL-U parser → SQLite
      master (pilot texts = those in the 2021 dump) → align + coverage diff → legacy/clean exports →
      validate. Data layer first; dashboards (M7) later.
- [ ] **(standing) Reconcile + push VisualDCS.** Local `main` has diverged from `origin/main`
      (ahead 33+ / behind 24); integrate the remote (codex migration + Dependabot) before pushing.

## 🚧 Current Work-In-Progress (WIP)

- [ ] **2021/2026 split — in progress (uncommitted).** Renamed `src/DCS-data` → `src/DCS-data-2021`
      (LFS rules re-pointed; verified pointers intact); created `src/DCS-data-2026/` with the
      comparison/import/tracker docs + sample; updated all cross-folder doc links + script paths.
      **Pending:** the `conllu` submodule (needs the pushed data repo), the new
      `src/DCS-data-2026/README.md` (version differences), and the final commit.

## 🧠 Dev Notes & Hypotheses

<!-- Persistent bugs, architectural pivots, things future-you needs to know. Keep dated. -->

- **2026-06-06 — final 2026 architecture: dedicated data repo + submodule.** The 1.2 GB CoNLL-U corpus
  is **not** committed into VisualDCS (would bloat it and blow the LFS quota). It lives in its own
  normal-git repo `gasyoun/dcs-conllu` (no LFS — text packs to a few hundred MB), pinned at `04e0778`,
  mounted as a submodule at `src/DCS-data-2026/conllu`. Supersedes the earlier "clone to a sibling dir"
  idea. `check_conllu_updates.py` watches upstream for commits after the pin.
- **2026-06-06 — the import is feasible because both DCS distributions share `LemmaId`.** Verified by
  `compare_dcs_formats.py`: the integer IDs in `0.csv` are exactly the CoNLL-U `MISC: LemmaId=` values
  for the same verse — the join key for the whole import. Caveats: granularity differs (1 relational
  line ↔ N CoNLL-U sentences); old per-row PKs ≠ CoNLL-U `OccId` (refresh re-derives, can't patch);
  old `10.csv` morphology is numeric codes (best-effort map, keep the UD master lossless); dependency
  syntax (`HEAD`/`DEPREL`) is present only for **Vedic Treebank** chapters (e.g. `Ṛgveda`).
- **2026-06-06 — local `main` has diverged from `origin/main`.** Don't push blind: origin carries 24
  commits (codex migration merge + Dependabot) the local branch lacks; integrate first.
- **2026-06-05 — `src/DCS-data-2021/` large-file strategy.** `10.csv`/`10.txt` exceed GitHub's 100 MB
  limit → git-ignored, committed as `*.part###` (rebuild via `rejoin.bat`); 11 large files via Git LFS.
  Full record in `src/DCS-data-2021/DCS-data-CLEANUP.md`.

## ✅ Completed (Recent only)

<!-- Move items here as they're checked off. Trim aggressively — only the recent few. -->

- **2026-06-06 — Split DCS-data into 2021 (DB export) and 2026 (CoNLL-U):**
  - `git mv src/DCS-data → src/DCS-data-2021`; re-pointed the 8 LFS `.gitattributes` rules; verified the
    LFS files stayed pointers (`0.csv` index blob = 133 bytes, `check-attr` = `lfs`).
  - Created `src/DCS-data-2026/` (comparison, import plan, tracker, sample); fixed all cross-folder doc
    links + the scripts' default paths; updated root `README.md` / `CLAUDE.md` / `CHANGELOG.md`.
  - Extracted upstream `conllu/` into a sibling `dcs-conllu/` repo (PROVENANCE.md + EOL guard); removed
    the 877 MB leftover clone.
- **2026-06-06 — DCS data documentation + import plan:** provenance README, verified format comparison
  (`compare_dcs_formats.py` + `DCS_FORMAT_COMPARISON.md` + sample), and the import plan + roadmap.
  Committed earlier: `51022f9` · `eb5ca53` · `72e7dce` (those landed at the old `src/DCS-data/` path).
- **2026-06-05 — Prepared `src/DCS-data-2021/` for GitHub** — split the >100 MB files, set up Git LFS,
  deduped 8 identical `.txt`s, renamed `DSC-data` → `DCS-data`.
