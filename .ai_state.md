# Project Objective: Import current DCS CoNLL-U updates into the relational export (pilot-first)

<!-- Replace the bracket above with the current high-level goal for this session. -->

## ➡️ Next Steps (Queue)

- [ ] **M8 — Land** per `src/DCS-data-2026/DCS_CONLLU_IMPORT_PLAN.md`: M1–M7 ✅ → **M8** finalize docs
      (README/CHANGELOG note the 2026 pipeline + the GitHub Release), confirm the source-SHA pin, tidy
      `.ai_state`. The full master is regenerable (`import_dcs_conllu.py --all`) + published as a Release.
- [ ] (optional) `git lfs prune` to drop any leftover LFS objects in `.git/lfs` now that the data is
      plain blobs.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (no active task — infrastructure done; **M1** is the next build step)

## 🧠 Dev Notes & Hypotheses

<!-- Persistent bugs, architectural pivots, things future-you needs to know. Keep dated. -->

- **2026-06-06 — local `main` had been UNRELATED to `origin/main` (now reconciled & pushed).** Local
  `main` had been re-committed on a fresh root, so it shared **no** ancestor with origin — it was a
  duplicate of origin's 24 commits + the new DCS work. Fixed by replaying only the new commits onto
  `origin/main` (cherry-pick in a worktree) → fast-forward push (`3844b33..cf6f622`). If `main` ever
  looks diverged with a different root again, **replay onto origin, don't merge**.
- **2026-06-06 — DCS-data large files are plain git blobs now, NOT LFS.** Converted out of LFS
  (`git lfs migrate export`) to dodge the over-quota LFS storage. They live in `src/DCS-data-2021/` as
  normal blobs (repo ~230 MB heavier; GitHub warns about >50 MB files but doesn't block). **Do not
  re-add LFS.** `.gitattributes` marks them `-text` to avoid CRLF rewrites.
- **2026-06-06 — 2026 CoNLL-U = submodule + dedicated repo.** Full 1.2 GB corpus in `gasyoun/dcs-conllu`
  (normal git, no LFS), pinned `04e0778` (2026-03-05), mounted at `src/DCS-data-2026/conllu`.
  `check_conllu_updates.py` watches upstream for commits after the pin.
- **2026-06-06 — the import is feasible because both DCS distributions share `LemmaId`.** Verified by
  `compare_dcs_formats.py`: `0.csv`'s integer IDs ARE the CoNLL-U `LemmaId`s. Caveats for M1: granularity
  differs (1 relational line ↔ N CoNLL-U sentences); old per-row PKs ≠ `OccId` (refresh re-derives);
  old `10.csv` morphology is numeric codes (best-effort map, keep UD master lossless); dependency
  syntax only on Vedic Treebank chapters (e.g. `Ṛgveda`).

## ✅ Completed (Recent only)

<!-- Move items here as they're checked off. Trim aggressively — only the recent few. -->

- **2026-06-06 — M7 done: dashboard widgets regenerated.** `regen_widgets.py` → `widgets/*.json`
  (gitignored) + `reports/m7_widgets.md`: corpus stats, verb distribution both ways (UD-native + legacy
  38-cat via the M4 map), morph_pn, tense_case, conc_totals, collocations. 1,007,361 verbs; PPP 216,803
  (≈2021's 233,080). Documented limits: UD Tense=Past conflates aorist/perfect; genres/dates + curated
  anki/passages need external inputs. HTML untouched. M8 (land) next.
- **2026-06-06 — M6 done: full corpus master + Release.** `import_dcs_conllu.py --all` → `dcs_full.sqlite`
  (270 texts, 5,688,416 tokens, 754,726 sentences, 74 treebank; ~880 MB / 287 MB gz). `validate.py --all`
  ALL PASS: coverage 270/98,606 (matches upstream), cross-walk 0, spot 0, integrity clean. **Caught +
  fixed a scaling bug:** sent_id collides within chapters (449 dropped) → synthetic sentence PK + token
  `sentence_id` link; position-based validation. Full DB → GitHub Release. M7 (dashboards) next.
- **2026-06-06 — M5 done: validation suite + CI gate.** `validate.py` (+ `.github/workflows/
  dcs-validate.yml`) → `reports/m5_validation.md`: cross-walk at scale, integrity, idempotency (DB built
  twice = identical hash), 50-verse full-token spot checks — ALL PASS. **Caught + fixed a real bug:**
  `occ_id` isn't unique (corpus reuses it across sub-sentences) → switched to a synthetic `id` PK + plain
  INSERT (no tokens lost; 134,047 now). M6 (scale to all 270) next.
- **2026-06-06 — M4 done: exports + code map.** `export_master.py` → clean CSVs (sentences/tokens/
  lemmas/tokens_wide) + legacy (`_8.csv`/`0.csv`/best-effort `10.csv`) + `reports/m4_exports.md`.
  `_8.csv` overlaps 2021 cleanly (14,294 shared); learned the verb tense/mood **code map** (31/33 codes
  → UD; e.g. code 1=Present Active→Pres|Ind, 24=Present Passive→Pres|Pass). 10.csv's 2021-internal IDs
  aren't reproducible from 2026 (documented). M5 (validate) next.
- **2026-06-06 — M3 done: coverage diff.** `coverage_diff.py` → `reports/coverage_diff.md`: 2021↔2026 =
  246→270 texts (30 added, mostly Vedic; 6 only-2021 incl. a rename), lemmas 91,406→98,606 (89.3%
  Jaccard), pilot count deltas confirm tokens stay ~constant while sentences re-segment. M4 next.
- **2026-06-06 — M2 done: SQLite master (pilot).** `import_dcs_conllu.py` builds `dcs.sqlite` —
  flatten-all schema (every FEATS/MISC key a column), `lemma` table from `dictionary.csv` (180,176
  entries), 134,027 tokens over 13 pilot texts (incl. Ṛgveda + Arthaśāstra treebanks), 0 orphan
  lemma_ids. DB is gitignored (regenerable). M3 (align + coverage diff) next.
- **2026-06-06 — M1 done: full CoNLL-U parser.** `src/DCS-data-2026/parse_conllu.py` — lossless reader
  (MWT, all FEATS/MISC, HEAD/DEPREL, doc+sentence metadata) → one JSONL per text in `staging/`
  (gitignored). Gate passed: Meghadūta (3393 tok) + Ṛgveda treebank (264 dependency arcs) parse with
  **0 column errors**. M2 (SQLite master) is next.
- **2026-06-06 — Reconciled + pushed `main` to GitHub.** `main` = origin's canonical 24 commits
  (incl. PR #4) + the DCS work, de-LFS'd, fast-forward pushed. Both repos live: `gasyoun/VisualDCS`
  `@ cf6f622` and `gasyoun/dcs-conllu` `@ 9310e7b6`.
- **2026-06-06 — Split DCS-data into `2021` (DB export) + `2026` (CoNLL-U + submodule)** with full docs:
  provenance README, verified format comparison, import plan + roadmap, version-differences README,
  update tracker.
- **2026-06-05 — Prepared `src/DCS-data-2021/` corpus** — split the >100 MB files, deduped 8 identical
  `.txt`s, renamed `DSC-data` → `DCS-data`.
