# Project Objective: Commit the `src/DCS-data/` raw corpus assets to GitHub (large-file handling)

<!-- Replace the bracket above with the current high-level goal for this session. -->

## ➡️ Next Steps (Queue)

- [ ] Launch the GitHub Desktop commit + push of `src/DCS-data/` (split parts + LFS files).
- [ ] After push, confirm on GitHub that the 11 LFS files show the "Stored with Git LFS" badge (not raw blobs).
- [ ] Keep an eye on the GitHub free LFS quota (1 GB storage / 1 GB-month bandwidth); current tracked total ≈ 760 MB.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (no active task)

## 🧠 Dev Notes & Hypotheses

<!-- Persistent bugs, architectural pivots, things future-you needs to know. Keep dated. -->

- **2026-06-05 — `src/DCS-data/` large-file strategy.** Folder was renamed `DSC-data` → `DCS-data`
  (correct acronym, matches repo name VisualDCS / Digital Corpus of Sanskrit). Full record in
  `src/DCS-data/DCS-data-CLEANUP.md`.
  - `10.csv` (189 MB) and `10.txt` (294 MB) exceed GitHub's 100 MB limit → kept locally but
    **git-ignored**; committed instead as line-boundary split parts (`10.{csv,txt}.part*`, each ≤ 99 MiB).
    Rebuild with `src/DCS-data/rejoin.bat`. Reconstruction is MD5-verified.
  - `10.csv` and `10.txt` are **different files** (bare CSV vs SQL-tuple format), not duplicates — both kept.
  - Large committed files (split parts + 6 standalone 50–99 MB files) are tracked via **Git LFS**.
    Rules live in repo-root `.gitattributes` using **full `src/DCS-data/...` paths** — important because
    the `!7.csv` filename's leading `!` would be read as a glob negation in a bare pattern, and because
    a folder rename silently breaks any path-scoped LFS rules (had to re-point DSC→DCS after the rename).
  - 8 `.txt` files that were byte-identical to their `.csv` twin were deleted (kept the `.csv`).

## ✅ Completed (Recent only)

<!-- Move items here as they're checked off. Trim aggressively — only the recent few. -->

- **2026-06-05** — Prepared `src/DCS-data/` for GitHub:
  - Split the two >100 MB files (`10.csv`, `10.txt`) into ≤ 99 MiB line-boundary parts; verified byte-exact rebuild.
  - Deleted 8 redundant `.txt` exact-duplicates (kept the identical `.csv`).
  - Added `.gitignore` (excludes the >100 MB originals) and `rejoin.bat` (rebuilds them).
  - Set up **Git LFS** for the 11 large committed files (split parts + 6 standalone); verified only the
    intended files resolve to the `lfs` filter.
  - Folder renamed `DSC-data` → `DCS-data`; fixed all references (`.gitattributes`, `.gitignore`,
    `rejoin.bat`, and renamed `DSC-data-CLEANUP.md` → `DCS-data-CLEANUP.md`) and re-verified LFS tracking.
