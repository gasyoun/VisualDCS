# Project Objective: DCS CoNLL-U → SQLite (M1–M8) + research-archive → archive.sqlite (M9), feeding downstream consumers

## ➡️ Next Steps (Queue)

- [ ] **M9 bridges (4, each its own executable handoff, DRAFTED — run them next).** `archive.sqlite`
      exists (Release [`archive-2026-07`](https://github.com/gasyoun/VisualDCS/releases/tag/archive-2026-07)).
      Each bridge is a separate future session; the verbatim start line is inside each file:
  - **B1:** [`OPUS_kosha_pwgru_frequency_layer.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/OPUS_kosha_pwgru_frequency_layer.md) — period/text freq → kosha layer + pwg_ru slice ordering.
  - **B2:** [`OPUS_Sundara_crosstext_apparatus.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/OPUS_Sundara_crosstext_apparatus.md) — parallels (Rāmāyaṇa id=248) → Sundara 3rd apparatus layer.
  - **B3:** [`OPUS_SanskritKaraoke_subhashita_feed.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/OPUS_SanskritKaraoke_subhashita_feed.md) — subhāṣita → karaoke verse feed.
  - **B4:** [`OPUS_Zaliznyak_inflection_index.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/OPUS_Zaliznyak_inflection_index.md) — grammar tables → pwg_ru nominal inflection index.
- [ ] (optional) `git lfs prune` to drop any leftover LFS objects in `.git/lfs` now that the data is
      plain blobs.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (none — the M1–M8 DCS import pipeline is complete and pushed)

## 🧠 Dev Notes & Hypotheses

<!-- Persistent bugs, architectural pivots, things future-you needs to know. Keep dated. -->

- **2026-07-02 — M9 archive→SQLite DONE (Opus 4.8, `claude-opus-4-8`).** One loader
  [`src/DCS-data-2026/import_archive.py`](https://github.com/gasyoun/VisualDCS/blob/main/src/DCS-data-2026/import_archive.py)
  (subcommand per dataset) → gitignored `archive.sqlite` (Release `archive-2026-07`). Report:
  [`reports/m9_archive_ingest.md`](https://github.com/gasyoun/VisualDCS/blob/main/src/DCS-data-2026/reports/m9_archive_ingest.md).
  Tables: `parallels` (fulltext 154,304 GOOD+PARTLY **+ stop-word ~40.5M**), `parallel_text`
  (146 texts, 95.9% dcs-linked), `text_names` (128, authoritative Приложение 2 id-list),
  `period_freq`/`text_freq`/`core_vocab` (~635k/286k/27k), `subhashita` (7,537) +
  `subhashita_ramayana` (8,941). **Key gotchas learned:** (1) parallel-project text ids are
  project-internal, NOT DCS text_ids (Acintyastava=10≠415); crosswalk via each file's constant
  first-field abbrev, then link to `dcs_full` by name. (2) CSV grammar = `src_ref;pada;text` +
  N×`ref;text;GOOD|PARTLY;matched`; quality-anchored parser recovers 271 malformed rows, zero
  silent loss. (3) stop-word run = 40.5M rows (264× fulltext) → stored WITHOUT verse text
  (`store_text=False`) + periodic commit/WAL-checkpoint (a single final commit ballooned WAL to
  5GB). (4) subhāṣita translation is GERMAN not Russian → `translation_de` (don't invent `_ru`).
- **2026-07-02 — research archive is TRACKED now; PART deleted deliberately.** The 13-batch
  "Back up derived-data/non-derived research archive" commits (Claude Sonnet 5, `claude-sonnet-5`)
  put both buckets in git (>95MB files as 7z split volumes, `RESTORE_SPLIT_FILES.md`;
  `non-derived/Zalizniak/GH/` nested repos excluded). `PARA/VSE/PART/` (11.7GB, full 245-text
  stop-word parallel run) was **deliberately deleted by M.G.** (confirmed 02-07-2026) — only the
  partial 113-text `Stopovye/` stop-word run survives. Five stale "untracked" doc claims fixed
  same day (both INDEX.md + both bucket README.md + root README.md).
- **2026-06-06 — local `main` had been UNRELATED to `origin/main` (now reconciled & pushed).** Local
  `main` had been re-committed on a fresh root, so it shared **no** ancestor with origin — it was a
  duplicate of origin's 24 commits + the new DCS work. Fixed by replaying only the new commits onto
  `origin/main` (cherry-pick in a worktree) → fast-forward push (`3844b33..cf6f622`). If `main` ever
  looks diverged with a different root again, **replay onto origin, don't merge**.
- **2026-06-06 — DCS-data large files are plain git blobs now, NOT LFS.** Converted out of LFS
  (`git lfs migrate export`) to dodge the over-quota LFS storage. They live in `src/DCS-data-2021/` as
  normal blobs (repo ~230 MB heavier; GitHub warns about >50 MB files but doesn't block). **Do not
  re-add LFS.** `.gitattributes` marks them `-text` to avoid CRLF rewrites.
- **2026-06-06 — 2026 CoNLL-U = submodule + dedicated repo.** Full 1.2 GB corpus in `gasyoun/dcs-conllu`
  (normal git, no LFS), pinned `04e0778` (2026-03-05), mounted at `src/DCS-data-2026/conllu`.
  `check_conllu_updates.py` watches upstream for commits after the pin.
- **2026-06-06 — the import is feasible because both DCS distributions share `LemmaId`.** Verified by
  `compare_dcs_formats.py`: `0.csv`'s integer IDs ARE the CoNLL-U `LemmaId`s. Caveats for M1: granularity
  differs (1 relational line ↔ N CoNLL-U sentences); old per-row PKs ≠ `OccId` (refresh re-derives);
  old `10.csv` morphology is numeric codes (best-effort map, keep UD master lossless); dependency
  syntax only on Vedic Treebank chapters (e.g. `Ṛgveda`).

## ✅ Completed (Recent only)

<!-- Move items here as they're checked off. Trim aggressively — only the recent few. -->

- **2026-07-03 — `subhashita` table exported to SanskritLexicography (Sonnet 5 `claude-sonnet-5`).**
  New [`src/DCS-data-2026/export_subhashita_jsonl.py`](https://github.com/gasyoun/VisualDCS/blob/main/src/DCS-data-2026/export_subhashita_jsonl.py)
  exports the full 7,537-saying Böhtlingk *Indische Sprüche* D4 table (from `archive.sqlite`, gitignored)
  to a plain-text edition in the sibling repo:
  [`SanskritLexicography/IndischeSprueche/data/indische_sprueche.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/IndischeSprueche/data/indische_sprueche.jsonl).
  `subhashita_ramayana` intentionally excluded (looks positional, not part of Böhtlingk's own
  apparatus). Registered in `Uprava/PROJECT_INTERLINKS.md`; PWG/PWK citation crosswalk scoped as
  [Uprava H143](https://github.com/gasyoun/Uprava/blob/main/handoffs/H143_pwg_pwk_indische_sprueche_crosswalk.md).

- **2026-07-03 — derived PPP asset added (Opus 4.8 `claude-opus-4-8`).** Extracted the "Past Passive
  Participle" subset of the verbal-forms DB (`derived-data/Glagolnye-formy/Bazadannyh-glagolnyh-form-Korpusa/`)
  → [`dcs_ppp_verified.tsv`](https://github.com/gasyoun/VisualDCS/blob/main/derived-data/Glagolnye-formy/Bazadannyh-glagolnyh-form-Korpusa/dcs_ppp_verified.tsv)
  (5,181 corpus-attested PPP forms + counts) + provenance `.md`, registered in
  [`derived-data/INDEX.md`](https://github.com/gasyoun/VisualDCS/blob/main/derived-data/INDEX.md). Now
  consumed by WhitneyRoots' decisions doc (feed in Uprava `PROJECT_INTERLINKS.md`). Trust form+count;
  class/voice are DCS lexicon metadata.

- **2026-07-02 — M9 archive→SQLite DONE & MERGED (PR [#8](https://github.com/gasyoun/VisualDCS/pull/8), Opus 4.8 `claude-opus-4-8`).**
  [`import_archive.py`](https://github.com/gasyoun/VisualDCS/blob/main/src/DCS-data-2026/import_archive.py) →
  `archive.sqlite` (167 MB, Release [`archive-2026-07`](https://github.com/gasyoun/VisualDCS/releases/tag/archive-2026-07))
  + regenerable `archive_stopword.sqlite` (40.5M rows). Report:
  [`reports/m9_archive_ingest.md`](https://github.com/gasyoun/VisualDCS/blob/main/src/DCS-data-2026/reports/m9_archive_ingest.md).
  4 bridge handoffs drafted; PROJECT_INTERLINKS + GTD updated. Details in Dev Notes above.
- **2026-06-26 — A38 publication skeleton moved onto `main`.** Cherry-picked the A38 canonical
  byline/data descriptor skeleton from `fix/dcs-lemma-anusvara-validation` onto `main` and rebased
  onto `origin/main`; no publication rewrite or large-data cleanup was attempted. No tests run
  (doc-only change). Local untracked `docs/Histoire_de_la_langue_sanskrite_Renou_Louis.pdf` and
  `src/dcs_full.sqlite` were preserved untouched.
- **2026-06-27 — Corpus/genre dashboard wired.** Added `dcs_corpus_dashboard.html`
  consuming tracked `visual/corpus_stats_widget.json`, `visual/dcs_genres.json`, and
  `visual/dcs_texts_clean.json`; the index cards "Корпусная статистика" and
  "DCS: 17 жанров" now open it. Verification: local HTTP smoke for dashboard,
  index, and JSON asset returned 200 (`dashboardBytes=8893`). Playwright was not
  available in this repo environment.
- **2026-06-06 — M8 done: landed. M1–M8 COMPLETE.** Full master published as a GitHub Release
  (`dcs-full-2026-03-05`, 287 MB gz; not committed — regenerable). README gains the 2026 source entry +
  the stale Git-LFS note fixed; added `CHANGELOG.md`; source pinned to `gasyoun/dcs-conllu @ 04e0778`.
- **2026-06-06 — M7 done: dashboard widgets regenerated.** `regen_widgets.py` → `widgets/*.json`
  (gitignored) + `reports/m7_widgets.md`: corpus stats, verb distribution both ways (UD-native + legacy
  38-cat via the M4 map), morph_pn, tense_case, conc_totals, collocations. 1,007,361 verbs; PPP 216,803
  (≈2021's 233,080). Documented limits: UD Tense=Past conflates aorist/perfect; genres/dates + curated
  anki/passages need external inputs. HTML untouched. M8 (land) next.
- **2026-06-06 — M6 done: full corpus master + Release.** `import_dcs_conllu.py --all` → `dcs_full.sqlite`
  (270 texts, 5,688,416 tokens, 754,726 sentences, 74 treebank; ~880 MB / 287 MB gz). `validate.py --all`
  ALL PASS: coverage 270/98,606 (matches upstream), cross-walk 0, spot 0, integrity clean. **Caught +
  fixed a scaling bug:** sent_id collides within chapters (449 dropped) → synthetic sentence PK + token
  `sentence_id` link; position-based validation. Full DB → GitHub Release. M7 (dashboards) next.
- **2026-06-06 — M5 done: validation suite + CI gate.** `validate.py` (+ `.github/workflows/
  dcs-validate.yml`) → `reports/m5_validation.md`: cross-walk at scale, integrity, idempotency (DB built
  twice = identical hash), 50-verse full-token spot checks — ALL PASS. **Caught + fixed a real bug:**
  `occ_id` isn't unique (corpus reuses it across sub-sentences) → switched to a synthetic `id` PK + plain
  INSERT (no tokens lost; 134,047 now). M6 (scale to all 270) next.
- **2026-06-06 — M4 done: exports + code map.** `export_master.py` → clean CSVs (sentences/tokens/
  lemmas/tokens_wide) + legacy (`_8.csv`/`0.csv`/best-effort `10.csv`) + `reports/m4_exports.md`.
  `_8.csv` overlaps 2021 cleanly (14,294 shared); learned the verb tense/mood **code map** (31/33 codes
  → UD; e.g. code 1=Present Active→Pres|Ind, 24=Present Passive→Pres|Pass). 10.csv's 2021-internal IDs
  aren't reproducible from 2026 (documented). M5 (validate) next.
- **2026-06-06 — M3 done: coverage diff.** `coverage_diff.py` → `reports/coverage_diff.md`: 2021↔2026 =
  246→270 texts (30 added, mostly Vedic; 6 only-2021 incl. a rename), lemmas 91,406→98,606 (89.3%
  Jaccard), pilot count deltas confirm tokens stay ~constant while sentences re-segment. M4 next.
- **2026-06-06 — M2 done: SQLite master (pilot).** `import_dcs_conllu.py` builds `dcs.sqlite` —
  flatten-all schema (every FEATS/MISC key a column), `lemma` table from `dictionary.csv` (180,176
  entries), 134,027 tokens over 13 pilot texts (incl. Ṛgveda + Arthaśāstra treebanks), 0 orphan
  lemma_ids. DB is gitignored (regenerable). M3 (align + coverage diff) next.
- **2026-06-06 — M1 done: full CoNLL-U parser.** `src/DCS-data-2026/parse_conllu.py` — lossless reader
  (MWT, all FEATS/MISC, HEAD/DEPREL, doc+sentence metadata) → one JSONL per text in `staging/`
  (gitignored). Gate passed: Meghadūta (3393 tok) + Ṛgveda treebank (264 dependency arcs) parse with
  **0 column errors**. M2 (SQLite master) is next.
- **2026-06-06 — Reconciled + pushed `main` to GitHub.** `main` = origin's canonical 24 commits
  (incl. PR #4) + the DCS work, de-LFS'd, fast-forward pushed. Both repos live: `gasyoun/VisualDCS`
  `@ cf6f622` and `gasyoun/dcs-conllu` `@ 9310e7b6`.
- **2026-06-06 — Split DCS-data into `2021` (DB export) + `2026` (CoNLL-U + submodule)** with full docs:
  provenance README, verified format comparison, import plan + roadmap, version-differences README,
  update tracker.
- **2026-06-05 — Prepared `src/DCS-data-2021/` corpus** — split the >100 MB files, deduped 8 identical
  `.txt`s, renamed `DSC-data` → `DCS-data`.
