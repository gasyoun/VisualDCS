# Nominal paradigm — Sangram G2 reconciliation (H1472)

_Auto-generated by `gen_paradigm_nominal.py`. Cross-checks this asset against SanskritGrammar's Sangram G2 declension-cell coverage ([lemma_cell_coverage.csv](https://github.com/gasyoun/SanskritGrammar/blob/main/sangram/data/declension_cell_coverage/lemma_cell_coverage.csv), H1048), which reads the same pinned `dcs_full.sqlite` with the same 24-cell matrix and the same `lemma_id` keying. Disagreements are logged here, never silently resolved._

**Scope difference, by design:** G2's universe is `upos='NOUN'` only; this asset also takes `ADJ`, because the roadmap brief asks for noun **and adjective** paradigms. The comparison below is therefore run on the shared NOUN-only subset, and the ADJ-only surplus is reported separately rather than counted as a disagreement.

## Shared NOUN subset

- **57,144** lemma_ids agree exactly on both token count and attested-cell count.
- **0** disagree.
- **0** NOUN lemma_ids here are absent from the G2 CSV.
- **0** G2 lemma_ids are absent from this asset's NOUN set.
- **21,802** ADJ-only lemma_ids are this asset's own surplus (outside G2's universe by design, not a disagreement).
- **0** lemma_ids carry BOTH `NOUN` and `ADJ` tokens in the master; they are compared on their NOUN tokens only.

## Class re-bucketing vs G2's `stem_final`

Every lemma_id whose class differs from G2's tag, and why. The only intended source of difference is the H1472 `-ant` extension pulling stems out of G2's `other_consonant` residue; anything else in this table is an unintended divergence and should be treated as a defect.

| G2 `stem_final` | this asset | lemma_ids | intended? |
|---|---|--:|---|
| `other_consonant` | `ant` | 279 | yes — `-ant` extension |


_Auto-generated by gen_paradigm_nominal.py._
